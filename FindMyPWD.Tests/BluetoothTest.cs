using NUnit.Framework;
using FindMyPWD.Helper;

namespace Bluetooth.Tests
{
    public class Tests
    {
        [SetUp]
        public void Setup()
        {
        }
        /*
        [Test]
        public void TestCheckBLE() {
            BLEScanneHelper BLEHelper = new BLEScanneHelper();
            string BLE = BLEHelper.CheckBLE().ToString();
            Assert.Equals(BLE,"");
        }

        [Test]
        public void TestScanBLE() {
            BLEScanneHelper BLEHelper = new BLEScanneHelper();
            Assert.Pass();
        }
        */
    }
}